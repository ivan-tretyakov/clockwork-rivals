# Production Node server (set in the host environment)
PORT=2567
DATA_DIR=.local
# ALLOWED_ORIGIN=https://your-clockwork-host.example
# Only needed if the browser app and room server use different origins:
# VITE_ROOM_SERVER=wss://your-room-host.example
